var React = require('react');
var ReactDOM = require('react-dom');
var ContentManager = require('./components/ContentManager.jsx');

ReactDOM.render(<ContentManager />, document.getElementById('content'));
